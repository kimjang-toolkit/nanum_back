import { ProductExtractReq } from "@interface/product";

export const extractProductInfoSRV = async (productExtactReq: ProductExtractReq) => {
  // const extractedProductInfo = await extractProductInfoDAO(productExtactReq);
  // return extractedProductInfo;
};